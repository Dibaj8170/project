<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dibby.bebek">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity"
            android:theme="@style/Baru">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.TentangBebek"
            android:theme="@style/Baru"/>

        <activity android:name=".view.Diagnosa"
            android:theme="@style/Baru"/>

        <activity android:name=".view.CaraBeternak"
            android:theme="@style/Baru"/>

        <activity android:name=".view.PenyakitBebek"
            android:theme="@style/Baru"/>
    </application>

</manifest>